for (let f = 0; f < fruits.length; f++) {
    if (fruits[f].includes("e")) {
      console.log(`This fruit ${fruits[f]} contains the letter e`);
    } else {
      console.log(`This fruit ${fruits[f]} does not contains the letter e`);
    }
  }
  
  let Colors = ["Red", "Blue", "Yellow", "Green", "Purple", "Orange", "Cyan"];
  
  for (let a = 0; a < Colors.length; a++) {
    if (Colors[a].includes("r")) {
      console.log(`This colors ${Colors[a]} contains the letter r`);
    } else {
      console.log(`This colors ${Colors[a]} does mot contains the letter r`);
    }
  }
  